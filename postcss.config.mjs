/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    "postcss-import": {},
    "postcss-nested": {},
    "postcss-flexbugs-fixes": {},
    autoprefixer: {}
  }
};

export default config;
